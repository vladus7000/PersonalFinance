# BUILD_PLAN — пошаговый план реализации PFA для агента-исполнителя

**Аудитория этого документа:** агент, пишущий код (Claude Sonnet), по одной задаче за сеанс.
**Основано на:** [doc.md](doc.md) (ТЗ v0.1), [MVP_PLAN.md](MVP_PLAN.md) (фазовый план), архитектурная валидация (Opus).
**Статус:** v1.0 — готов к исполнению.

> Читай раздел 0 (конвенции) ПЕРЕД любой задачей. Он фиксирует сквозные решения,
> нарушение которых приводит к переписыванию. Каждая задача самодостаточна, имеет
> критерий готовности (DoD) и зависимости. Выполняй задачи строго в порядке номеров
> внутри эпика; эпики — по порядку, если в задаче не указано иное.

---

# 0. Сквозные конвенции (обязательны для всех задач)

## 0.1. Зафиксированные архитектурные решения

1. **Деньги, количества, цены = `Decimal`** (пакет `decimal`), НИКОГДА `double`.
   Хранение в Drift — как TEXT (canonical decimal string). Агрегация (сумма капитала и т.п.)
   выполняется в Dart, не в SQL. Запрещено объявлять денежные/количественные поля как `double`/`REAL`.
2. **Реализованный P&L — weighted-average cost basis.** `Holding.averagePrice` пересчитывается
   при каждой покупке; при продаже: `realized = (sellPrice − averagePrice) × qtySold`, средняя
   цена не меняется. FIFO-лоты — вне scope (задел на будущее не требуется в схеме).
3. **Курсы и история капитала — таблицы с самого начала:** `exchange_rates` и `net_worth_snapshots`
   создаются в Epic 1. Без них не строится график капитала (§3.4, §2.23 doc.md).
4. **Слоистая, домен-центричная архитектура.** Вся финансовая математика — в чистом доменном
   слое без зависимостей от Flutter и Drift, и покрыта unit-тестами. UI и БД от домена зависят,
   не наоборот.
5. **Инвариант капитала (единственный источник истины).**
   `NetWorth = Σ(account.balance в primary) + Σ(holding.marketValue в primary) − Σ(debt.outstanding в primary)`.
   Баланс брокерского счёта = ТОЛЬКО кэш. Покупка актива атомарно уменьшает кэш и увеличивает
   количество холдинга — стоимость не считается дважды. Эта формула реализуется РОВНО в одном
   месте (`NetWorthCalculator`) и нигде не дублируется.
6. **Границы агрегата `FinancialDecision`:** это aggregate root. `FinancialOperation` = только
   перевод кэша между счетами (§5.2). Покупка/продажа = `InvestmentTransaction` со ссылкой на
   `decisionId?`. Каждая запись влияет на капитал строго один раз.
7. **Платформа MVP — только iOS.** Android/web/macOS не подключаются (проект создан с `--platforms=ios`).
   Не добавляй Android-специфичный код и не выполняй `flutter create` для других платформ без явного
   запроса пользователя.
8. **Ошибки — типизированные.** Application-слой возвращает `Result<T, Failure>` (freezed sealed
   union), а не бросает голые исключения. Исключения — только для настоящих аномалий (повреждение БД).
9. **Compat-принципы масс-маркета** из [MVP_PLAN.md](MVP_PLAN.md) §8 действуют: репозитории через
   интерфейсы; `ExchangeRateSource`/`WorkCalendarSource` как абстракции с `ManualSource`; `institution`
   как строка, не enum; i18n-каркас; онбординг как движок шагов; таблица `Debt` в схеме без UI;
   nullable `householdId` в таблицах капитала; единый `ImportSource` (`manual` в MVP); чтение только
   через Application-провайдеры.

## 0.2. Стек и версии

Flutter 3.44.x (stable, установлен), Dart 3.12.x. Пакеты (версии закреплены в E0.T2, фактически
установленные — см. `pubspec.yaml`):
`flutter_riverpod` + `riverpod_annotation` + `riverpod` (codegen; `riverpod` добавлен явно —
`Override`, нужный для `test/support/pump_app.dart`, живёт в `package:riverpod/misc.dart`, а не
реэкспортируется из `flutter_riverpod.dart`), `go_router`, `drift` + `sqlite3` (НЕ
`sqlite3_flutter_libs` — этот пакет официально EOL, `sqlite3` 3.x бандлит нативные библиотеки сам
через Dart native assets), `decimal`, `freezed_annotation` + `json_annotation`, `path_provider`,
`path`, `intl`, `flutter_localizations`, `meta`; dev: `build_runner`, `riverpod_generator`, `freezed`,
`json_serializable`, `drift_dev`, `mocktail`, `integration_test` (SDK, добавлен в E1.T3 — нужен для
проверки file-backed `AppDatabase` на реальном симуляторе, не только in-memory в unit-тестах).
Пакеты, добавляемые позже в своих эпиках (НЕ ставить раньше): `fl_chart` (E9), `file_picker` +
`open_filex`/просмотр PDF (E8), `flutter_local_notifications` (E10), `flutter_secure_storage` +
`local_auth` (E10).

**Известное отклонение:** `custom_lint`/`riverpod_lint` НЕ установлены (E0.T2). На момент установки
`custom_lint` требует `analyzer ^7`/`^8`, а `drift_dev 2.34.x` требует `analyzer ^13` — жёсткий
конфликт версий в экосистеме, не разрешается выбором версий пакетов. Это dev-only линтер, не влияет
на сборку/работу приложения. Переоценить при апдейте зависимостей (напр. в E13 или при мажорном
апдейте drift/riverpod).

**Любой новый пакет вне этого списка — только после явного согласия пользователя** (правило CLAUDE.md).

## 0.3. Команды

- Окружение сессии: PATH включает `~/development/flutter/bin` и `~/.gem/ruby/2.6.0/bin`, задан
  `RUBYOPT="-rlogger"` (нужно для CocoaPods под системный Ruby 2.6). В новом терминале они
  подхватываются из `~/.zprofile`.
- Кодогенерация: `dart run build_runner build --delete-conflicting-outputs`
  (в разработке — `dart run build_runner watch -d`).
- Анализ/тесты: `flutter analyze`, `flutter test`, интеграционные — `flutter test integration_test`.
- Сборка проверки: `flutter build ios --debug --simulator --no-codesign`.

## 0.4. Правила кода

- Код и комментарии — на английском; строки UI — только через l10n (ARB), без хардкода.
- Генерённые файлы (`*.g.dart`, `*.freezed.dart`, `*.drift.dart`) — в `.gitignore`, не коммитятся,
  создаются кодогенерацией и в CI.
- Каждый доменный движок — чистый, без I/O, с unit-тестами в `test/domain/engines/`.
- Каждая новая Drift-таблица деньги/кол-во хранит как TEXT + отдельная колонка `currency` (где
  применимо); вычисляемые величины сопровождаются `as_of`/`source` там, где это предусмотрено §5.11.
- Не коммить, не пуши, не создавай теги без явной просьбы пользователя.
- **Перед стейджингом файлов — проверять на чувствительные/личные данные**, которые могла молча
  добавить автогенерация (`flutter create`, Xcode, IDE): подписные идентичности (`DEVELOPMENT_TEAM`
  и другие ключи в `ios/*.pbxproj`), абсолютные локальные пути, личные email/логины, ключи/токены.
  Прецедент: E0 — `flutter create` сама вписала реальный Apple Developer Team ID в
  `project.pbxproj`, это ушло в первый коммит и потребовало переписывания истории. Проверять именно
  новые/пересозданные файлы этих категорий, не полагаясь только на то, что диф выглядит привычно.
- **Секреты будущего бэкенда** (E14.T6, doc.md §11.6) — только через `--dart-define` или локальный
  `.env`, который никогда не коммитится (превентивно уже в `.gitignore`, хотя реального `.env` и
  бэкенда пока нет). Ни при каких обстоятельствах не хардкодить ключ/токен в исходниках «временно».

## 0.5. Definition of Done (общий, применяется к КАЖДОЙ задаче)

`flutter analyze` без ошибок; кодогенерация проходит; новые/затронутые тесты зелёные;
`flutter build ios --debug --simulator --no-codesign` успешна; в задаче выполнен её частный DoD.

## 0.6. Тестовая архитектура

Зафиксировано после E1, чтобы объём тестов не расползался по стилю с ростом числа экранов и
модулей (E2–E13). Полная версия с обоснованием — в [ARCHITECTURE.md](ARCHITECTURE.md); здесь —
таблица-памятка и обязательные правила.

**Пирамида по слоям** — бизнес-логику проверяем один раз, на самом низком возможном слое;
виджет-тесты не пересчитывают бизнес-правила, только проверяют рендер состояний.

| Слой | Чем тестируем | Пример |
|---|---|---|
| `domain/engines/*` | Только фейки на интерфейсах, без БД | `test/domain/engines/currency_converter_test.dart` |
| `data/*` (repositories/sources/daos) | In-memory Drift (`NativeDatabase.memory()`), никогда файл/path_provider | `test/data/sources/manual_exchange_rate_source_test.dart` |
| `features/*/application/*` (Riverpod-контроллеры) | `ProviderContainer` + override репозиториев на фейки, без pump виджета | — (появится в E2) |
| `features/*/presentation/*` (экраны) | Widget-тесты через `test/support/pump_app.dart` | — (появится в E2) |
| `integration_test/` | Только то, что нельзя замокать (path_provider, биометрия, файлы) + сквозные user flow (E13.T3) | `integration_test/app_database_test.dart` |

**Обязательные правила:**

1. `test/` зеркалит путь в `lib/` 1:1 (например, `lib/domain/engines/x.dart` → `test/domain/engines/x_test.dart`). Файлы верхнего уровня `lib/` (`main.dart`) → `test/main_test.dart`.
2. **Фейки вместо моков** для простых интерфейсов — пиши руками (как `_FakeExchangeRateSource`), это нагляднее. `mocktail` — только для интерфейсов с большим числом методов, где ручной фейк громоздкий.
3. Общие тестовые обёртки/хелперы — в `test/support/`, не копировать boilerplate по файлам (`pump_app.dart` уже есть).
4. `integration_test/` не разрастается на рутинные экранные проверки — только платформенные зависимости и сквозные флоу.

---

# Карта эпиков

| Эпик | Тема | Соответствие MVP_PLAN | Зависит от |
|---|---|---|---|
| E0 | Каркас проекта, DI, роутер, CI, l10n | Phase 0 | — |
| E1 | Ядро: БД, money, курсы, движки-заготовки, бэкап | Phase 0–1 | E0 |
| E2 | Профиль + онбординг (движок шагов) | Phase 1 | E1 |
| E3 | Доход: источники, расчёт зарплаты, прогноз | Phase 1 | E2 |
| E4 | Финансовый цикл + подтверждение дохода | Phase 2 | E3 |
| E5 | Счета, снапшоты баланса, капитал | Phase 3 | E1 |
| E6 | План распределения (allocation engine) | Phase 2–3 | E4, E5 |
| E7 | Подушка и цели | Phase 4 | E5 |
| E8 | Инвестиции: активы, холдинги, сделки | Phase 5 | E5 |
| E9 | Финансовые решения + маршрут денег | Phase 6 | E5, E8 |
| E10 | Документы | Phase 7 | E9 |
| E11 | Dashboard + аналитика (графики) | Phase 8 | E4–E9 |
| E12 | Планировщик, уведомления, безопасность | Phase 9 | E4 |
| E13 | Edge cases, тесты, стабилизация | Phase 10 | все |
| E14 | Готовность к дистрибуции и эксплуатации | doc.md §11 (пост-MVP по духу, задачи дешёвые сейчас) | E0 |

---

# E0 — Каркас проекта

**Цель эпика:** запускаемое iOS-приложение со слоистой структурой, DI, роутером на 5 вкладок,
i18n-каркасом и CI. Экраны — пустые заглушки.

### E0.T1 — Структура каталогов и `.gitignore`
- **Контекст:** проект уже создан (`lib/main.dart` — дефолт). Нужно завести структуру из
  раздела 2.4 архитектурной оценки.
- **Действия:** создать дерево `lib/{app,core,domain,data,platform,features}/…` (см. структуру
  папок в анализе Opus); в `.gitignore` добавить `*.g.dart`, `*.freezed.dart`, `*.drift.dart`,
  `build/`, `.dart_tool/` (проверить, что уже не покрыто).
- **DoD:** дерево создано (пустые папки с `.gitkeep`), `flutter analyze` чист.
- **Зависимости:** —

### E0.T2 — Зависимости и codegen-конфиг
- **Действия:** добавить в `pubspec.yaml` пакеты из §0.2 (только группа «базовая», без E8–E10
  пакетов); настроить `build.yaml` при необходимости; прогнать `flutter pub get`.
- **DoD:** `flutter pub get` успешен; `dart run build_runner build` проходит (пусть пока без
  генерируемых источников); версии закреплены в `pubspec.lock`.
- **Зависимости:** E0.T1

### E0.T3 — l10n-каркас
- **Действия:** включить `flutter_localizations` + `intl`; создать `l10n.yaml`, `lib/core/l10n/app_en.arb`
  и `app_ru.arb` с 3–4 стартовыми строками; подключить `AppLocalizations` в `MaterialApp`.
- **DoD:** приложение стартует с локализованным заголовком; переключение locale работает; хардкод-строк
  в UI нет.
- **Зависимости:** E0.T2

### E0.T4 — Тема и дизайн-токены
- **Контекст:** §6 doc.md (тёмная тема, премиальный финансовый UI, семантика цветов §6.2).
- **Действия:** `lib/app/theme/` — `ThemeData` (dark по умолчанию), цветовые токены по §6.2
  (зелёный/синий/оранжевый/фиолетовый/красный/серый), типографика (крупные показатели). Абстракция
  токенов, чтобы палитра менялась в одном месте.
- **DoD:** заглушки экранов используют токены темы, не сырые `Color(...)`.
- **Зависимости:** E0.T2

### E0.T5 — Роутер и оболочка навигации
- **Контекст:** нижнее меню зафиксировано (вариант A, §8.2): Обзор / План / Счета / Решения / Ещё.
- **Действия:** `go_router` с `StatefulShellRoute.indexedStack` на 5 веток; для каждой — пустой
  экран-заглушка в `features/*/presentation`. «Ещё» ведёт к списку-заглушке (Инвестиции, Документы,
  Цели, Аналитика, Планировщик, Настройки).
- **DoD:** переключение вкладок работает, сохраняется состояние ветки; deep-link на маршрут открывает
  нужную вкладку.
- **Зависимости:** E0.T3, E0.T4

### E0.T6 — DI-корень (Riverpod)
- **Действия:** `ProviderScope` в `main`; `lib/app/di.dart` для корневых провайдеров (пока пусто/заготовка).
  `riverpod_lint`/`custom_lint` НЕ ставятся — см. известное отклонение ниже; перепроверить при апдейте зависимостей.
- **DoD:** приложение стартует через `ProviderScope`.
- **Зависимости:** E0.T5

### E0.T7 — CI (GitHub Actions)
- **Действия:** `.github/workflows/ci.yml`: setup Flutter 3.44.x → `pub get` → `build_runner build`
  → `flutter analyze` → `flutter test`. Без iOS-сборки на CI (экономия; проверяется локально).
- **DoD:** workflow валиден (yaml), шаги логически полны; (пуш — только по просьбе пользователя).
- **Зависимости:** E0.T2

---

# E1 — Ядро данных и расчётов

**Цель:** value objects денег, Drift-БД с миграциями и авто-бэкапом, таблицы курсов/снапшотов,
интерфейсы источников, каркасы движков. UI здесь нет.

### E1.T1 — `Result`/`Failure`
- **Действия:** `core/result/` — sealed `Failure` (freezed) с вариантами (`validation`, `notFound`,
  `conflict`, `storage`, `currencyMismatch`, …) и `Result<T>` (или использовать `dartz`-подобный свой
  тип; сторонний пакет — только с согласия).
- **DoD:** типы компилируются, покрыты минимальным тестом; используются далее вместо `throw`.
- **Зависимости:** E0.T2

### E1.T2 — Value object `Money` и `Quantity`
- **Контекст:** §0.1 п.1. `decimal` пакет. Порядок сознательно переставлен относительно исходного
  плана: `Money.add/subtract` возвращает `Failure` (`currencyMismatch`) при смешении валют, а значит
  `Result`/`Failure` (E1.T1) должен существовать раньше.
- **Действия:** `core/money/money.dart` — `Money{ Decimal amount; CurrencyCode currency }` c операциями
  (add/subtract только в одной валюте — иначе `Failure`), `Quantity` (Decimal, произвольная точность),
  `Percentage`, политика округления (round half-up, только на отображении). Парсинг/сериализация в/из
  canonical string.
- **DoD:** unit-тесты: арифметика, запрет смешения валют, round-trip string↔Money без потерь,
  крипто-точность (8+ знаков).
- **Зависимости:** E1.T1

### E1.T3 — Drift-БД, версия схемы, подключение
- **Действия:** `data/db/app_database.dart` — `@DriftDatabase`, `schemaVersion = 1`, открытие через
  `path_provider` в app-support каталоге. In-memory конструктор для тестов (`NativeDatabase.memory`).
- **DoD:** БД открывается на симуляторе; тест открывает in-memory БД.
- **Зависимости:** E1.T2

### E1.T4 — `Clock` и tz-aware время
- **Контекст:** порядок сознательно переставлен относительно исходного плана: авто-бэкап (ниже,
  теперь E1.T7) берёт дату из `Clock`, значит `Clock` должен существовать раньше.
- **Действия:** `core/time/clock.dart` — инъектируемый `Clock` (в проде — системное время, в тестах —
  фиксированное); helper для «сегодня» с учётом `timezone` профиля (пока UTC-заглушка до E2).
- **DoD:** движки и БД берут время только через `Clock`; тесты используют фиксированный Clock.
- **Зависимости:** E0.T2

### E1.T5 — Таблицы курсов и конвертация
- **Действия:** таблица `exchange_rates(from, to, rate TEXT, effective_date, source='manual', is_final)`;
  доменный `ExchangeRateSource` (интерфейс) + `ManualExchangeRateSource` (из таблицы); `CurrencyConverter`
  (домен) с политикой fallback (§5.9: если курса нет — вернуть результат с флагом `stale`/`missing`,
  не выдавать приблизительное за точное).
- **DoD:** unit-тесты конвертации: прямой курс, обратный, отсутствующий курс → помеченный результат.
- **Зависимости:** E1.T2, E1.T3

### E1.T6 — Таблица `net_worth_snapshots`
- **Действия:** `net_worth_snapshots(date, total_primary TEXT, breakdown JSON, source)`; DAO для записи/чтения
  диапазона. Запись вызывается из будущих сервисов по событиям (доход, обновление баланса, закрытие цикла).
- **DoD:** DAO пишет и читает снапшоты за период; тест на сериализацию breakdown.
- **Зависимости:** E1.T3

### E1.T7 — Авто-бэкап и стратегия миграций
- **Контекст:** данные локальные, сервера нет — потеря = катастрофа (анализ §1.3).
- **Действия:** перед применением миграции копировать файл БД в `backups/` с датой (дату брать из
  инъектируемого `Clock`, т.к. прямой `DateTime.now()` в некоторых окружениях запрещён — использовать
  `core/time/clock.dart`); `MigrationStrategy` с логированием версий.
- **DoD:** тест: миграция с v1→v1 (no-op) создаёт бэкап-копию; повреждать данные миграция не может.
- **Зависимости:** E1.T3, E1.T4

---

# E2 — Профиль и онбординг

**Цель:** пользователь проходит быстрый онбординг (движок шагов) и получает профиль + пустой Dashboard.

### E2.T1 — Таблица и репозиторий `UserProfile`
- **Контекст:** §4.1 doc.md. Добавить nullable `householdId` (compat №7).
- **Действия:** таблица `user_profiles`; `UserProfileRepository` (интерфейс в domain, Drift-impl в data);
  доменная сущность (freezed).
- **DoD:** CRUD профиля покрыт тестом на in-memory БД.
- **Зависимости:** E1.T3

### E2.T2 — Движок онбординга (step engine)
- **Контекст:** compat №5 — не жёсткий wizard, а список шагов с пропуском/дефолтами.
- **Действия:** `features/onboarding/application/` — модель шага (id, обязательность, валидность, данные),
  контроллер прохождения, поддержка «пропустить» и «назад». UI-шелл с прогрессом.
- **DoD:** можно пройти набор из ≥2 шагов вперёд/назад/пропустить; состояние сохраняется между шагами.
- **Зависимости:** E2.T1

### E2.T3 — Шаг 1: страна/валюта; Шаг 4: жизнь (агрегат)
- **Контекст:** §3.3 шаги 1 и 4; §2.8 (fixed/percent/range).
- **Действия:** экраны выбора основной валюты (+доп. валют) и режима расходов на жизнь; запись в профиль
  и заготовку настроек цикла. `institution`/валюты — из справочника-строк, не enum (compat №3).
- **DoD:** выбранные значения сохраняются в профиль; повторный вход показывает их.
- **Зависимости:** E2.T2

### E2.T4 — Экран первого запуска и завершение онбординга
- **Контекст:** §3.2, §3.3 «Завершение».
- **Действия:** welcome-экран (философия продукта, §3.2), финальное резюме, флаг `onboardingCompleted`,
  переход на Dashboard-заглушку. Роутер: если онбординг не пройден — редирект на него.
- **DoD:** первый запуск → онбординг → Dashboard; повторный запуск → сразу Dashboard.
- **Зависимости:** E2.T3

> Примечание: шаги онбординга 2–3 (доход), 5 (подушка), 6 (инвестиции), 7 (цель) добавляются как
> отдельные задачи в E3/E7/E8 по мере готовности соответствующих сущностей — движок шагов (E2.T2)
> позволяет дописывать их, не переделывая онбординг.

---

# E3 — Доход и расчёт зарплаты

**Цель:** источники дохода, детерминированный расчёт ожидаемой выплаты, экран прогноза.

### E3.T1 — Таблицы `IncomeSource`, `IncomeScheduleRule`
- **Контекст:** §4.2, §4.3. Валюты contract/payout раздельно (§5.8). `householdId` nullable.
- **Действия:** таблицы + репозиторий + доменные сущности; enum типов дохода; правила частей выплаты
  (доля/фикс, день, перенос с выходных).
- **DoD:** CRUD источника с ≥2 частями выплаты покрыт тестом.
- **Зависимости:** E1.T5, E2.T1

### E3.T2 — Движок `SalaryCalculator` (чистый домен)
- **Контекст:** §2.3 — САМАЯ ответственная логика. Спецификация должна быть детерминированной.
- **Действия:** `domain/engines/salary_calculator.dart`. Вход: правило источника + рабочие дни/отработанные
  дни (ручной ввод в MVP через `WorkCalendarSource` = ManualSource) + курс (через `ExchangeRateSource`) +
  политика округления. Выход: `SalaryForecast{ expectedDate, amountContract, amountPayout, rate, rateSource,
  workingDays, partIndex, isRateStale }`. Поддержать сценарии MVP: фиксированная; по рабочим дням; две части
  (%/фикс); контракт USD/EUR → выплата UAH по заданному курсу; удержания; перенос с выходного.
- **DoD:** таблица unit-тестов покрывает КАЖДЫЙ сценарий §2.3 (мин. по одному кейсу), включая неполный
  месяц и перенос даты; расчёт детерминирован при фиксированном Clock.
- **Зависимости:** E3.T1, E1.T5, E1.T4

### E3.T3 — Онбординг шаги 2–3 (доход) + провайдеры
- **Действия:** добавить в движок онбординга шаги «Доход» и «Правило расчёта» (§3.3); Application-провайдеры
  источников дохода.
- **DoD:** в онбординге можно завести источник дохода, он сохраняется и доступен после.
- **Зависимости:** E3.T2, E2.T2

### E3.T4 — Экран прогноза зарплаты
- **Контекст:** §3.7 (упрощённо: без графика курса — статичное поле курса).
- **Действия:** экран, показывающий разложение расчёта из `SalaryForecast` (период, дни, доля, курс,
  источник курса, суммы в двух валютах); действия «изменить курс/рабочие дни».
- **DoD:** для заведённого источника видно корректную дату и сумму следующей выплаты; изменение курса
  пересчитывает сумму.
- **Зависимости:** E3.T3

---

# E4 — Финансовый цикл и подтверждение дохода

**Цель:** цикл-месяц, привязка дохода к циклу, подтверждение факта, закрытие цикла.

### E4.T1 — Таблица `FinancialCycle` + `CycleFactory`
- **Контекст:** §4.8, §2.6, §5.5. Границы цикла — по `financialMonthStartDay` (анализ П7).
- **Действия:** таблица + сущность + state machine статусов (§2.6: future→planned→active→partiallyDone→
  awaitingClose→closed→archived — с допустимыми переходами); `CycleFactory` генерирует период (аккуратно
  с 29–31 числами). Один активный цикл (§5.5).
- **DoD:** тесты: генерация периода для разных `startDay`; запрет недопустимых переходов статуса;
  создание текущего цикла при завершении онбординга.
- **Зависимости:** E1.T4, E2.T4

### E4.T2 — Таблица `IncomeEvent` + привязка к циклу
- **Контекст:** §4.7. Правило принадлежности: по `expectedDate` (анализ П6), с возможностью ручного переноса.
- **Действия:** таблица + репозиторий; связь `financialCycleId`; статусы (expected/confirmed/…);
  поля expected/actual (сумма, курс, дата) раздельно.
- **DoD:** доход создаётся в статусе expected и привязывается к правильному циклу; тест на позднюю выплату
  (приходит в след. месяце, но принадлежит циклу по expectedDate).
- **Зависимости:** E4.T1, E3.T2

### E4.T3 — Экран подтверждения зарплаты
- **Контекст:** §3.8, user flow §3.23.
- **Действия:** экран ожидаемая/фактическая (сумма, курс, дата), выбор счёта зачисления (счета — из E5;
  до E5 допустима заглушка выбора). После подтверждения: событие дохода → confirmed, обновление
  цикла, запись `net_worth_snapshot`.
- **DoD:** подтверждение дохода меняет статус события, обновляет цикл, создаёт снапшот; отклонение
  план/факт видно.
- **Зависимости:** E4.T2, E1.T6

### E4.T4 — Экран финансового цикла
- **Контекст:** §3.6 (единый экран «План/Цикл» с режимами — §8.5). Здесь — режим «Цикл».
- **Действия:** статус, прогресс, ожидаемые/фактические доходы, список действий, кнопка «Завершить цикл»
  (§3.26 упрощённо: закрытие без развитой аналитики; при закрытии — снапшот и создание следующего цикла).
- **DoD:** user flow §3.23 проходится целиком; цикл закрывается вручную, создаётся следующий.
- **Зависимости:** E4.T3

---

# E5 — Счета и капитал

**Цель:** счета/хранилища, история балансов, расчёт капитала в основной валюте.

### E5.T1 — Таблицы `Account`, `AccountBalanceSnapshot`
- **Контекст:** §4.11, §4.12. `institution` — строка (compat №3); `householdId` nullable; `includeInNetWorth`,
  `capitalCategory`.
- **Действия:** таблицы + репозиторий + сущности; enum типов счёта; баланс как TEXT+currency.
- **DoD:** CRUD счёта и снапшота баланса на in-memory БД покрыт тестом.
- **Зависимости:** E1.T5

### E5.T2 — Экран счетов и карточка счёта
- **Контекст:** §3.9, §3.10. Группировка по типам. Карточки счетов в `design/dashboard-detail.png`
  (блок «Счета и хранилища») — референс для карточки счёта в списке.
- **Действия:** список с группами (банки/Wise/брокеры/крипто/наличные/депозиты/долги/архив), карточка
  счёта, ручное обновление баланса (создаёт снапшот + `net_worth_snapshot`).
- **DoD:** можно создать несколько счетов в разных валютах, обновить баланс; изменения видны.
- **Зависимости:** E5.T1, E1.T6

### E5.T3 — Движок `NetWorthCalculator`
- **Контекст:** §0.1 п.5 (инвариант). Единственная реализация формулы капитала.
- **Действия:** `domain/engines/net_worth_calculator.dart`: суммирует балансы счетов + рыночную стоимость
  холдингов (когда появятся в E8) − долги; конвертация через `CurrencyConverter`; возвращает
  `NetWorth{ totalPrimary, breakdown, isStale }` (stale, если хоть один курс отсутствует).
- **DoD:** тесты: мультивалютный расчёт; исключение счёта с `includeInNetWorth=false`; пометка stale при
  отсутствии курса; отсутствие двойного учёта (property-тест: покупка не меняет суммарный капитал, только
  структуру — добавить после E8, здесь — заглушка холдингов = 0).
- **Зависимости:** E5.T1, E1.T5

### E5.T4 — Онбординг шаг «основной счёт» + привязка дохода к счёту
- **Действия:** шаг онбординга для первого счёта; в экране подтверждения дохода (E4.T3) заменить заглушку
  выбора счёта реальным списком.
- **DoD:** доход зачисляется на реальный счёт, баланс которого растёт на сумму выплаты.
- **Зависимости:** E5.T2, E4.T3

---

# E6 — План распределения

**Цель:** движок распределения дохода и экран «План».

### E6.T1 — Спецификация и движок `AllocationEngine`
- **Контекст:** §2.7 — недоспецифицирован (анализ П4). СНАЧАЛА зафиксировать семантику в докстроке,
  затем код.
- **Действия:** `domain/engines/allocation_engine.dart`. Зафиксировать: правила применяются по `priority`
  (возр.); процент считается от валового дохода (решение пользователя — уточнить у него, если понадобится;
  по умолчанию — от валового); fixed раньше percent раньше residual; при нехватке денег — фиксированные
  урезаются с флагом `underfunded`, отрицательного остатка быть не может; условия («доход > ожидаемого»)
  вычисляются над подтверждённой суммой. Таблицы `AllocationRule`, `PlannedAllocation` (§4.9, §4.10).
- **DoD:** таблица unit-тестов: fixed/percent/residual/priority, нехватка средств, условие «доход выше/ниже
  ожидаемого», `freeAmount` = доход − Σ распределений. Детерминизм гарантирован.
- **Зависимости:** E4.T2, E5.T1

### E6.T2 — Экран «План» (режим плана единого экрана План/Цикл)
- **Контекст:** §3.5, §6.7.2 doc.md. Блоки: доходы, расходы на жизнь, распределение, правила.
  Экран «Стратегия» в `design/mockups-overview-strip.png` — референс верстки блоков доходов/
  расходов/правил/целей (это содержимое, не отдельная вкладка — см. §6.7.2 doc.md).
- **Действия:** отображение прогноза доходов, режима расходов на жизнь, результата `AllocationEngine`;
  редактирование правил распределения.
- **DoD:** после подтверждения дохода план показывает суммы по направлениям (жизнь/подушка/инвестиции/цели)
  и свободный остаток.
- **Зависимости:** E6.T1, E4.T4

---

# E7 — Подушка и цели

### E7.T1 — Таблицы `EmergencyFund`, `EmergencyFundComponent`, `Goal`
- **Контекст:** §4.20, §4.21, §4.22. `householdId` nullable.
- **Действия:** таблицы + репозитории + сущности; расчёт прогресса (текущая/целевая, % , месяцы покрытия,
  недостающая сумма, прогноз даты — простая линейная оценка).
- **DoD:** прогресс подушки и цели вычисляется тестом.
- **Зависимости:** E5.T1

### E7.T2 — Экран целей и подушки + шаги онбординга
- **Контекст:** §3.19, §3.3 шаги 5 и 7.
- **Действия:** экран списка целей и подушки с прогрессом; добавить шаги онбординга «Подушка» и «Цель».
- **DoD:** видно % выполнения подушки и ≥1 цели; онбординг заводит их.
- **Зависимости:** E7.T1, E2.T2

---

# E8 — Инвестиции

### E8.T1 — Таблицы `Asset`, `Holding`, `InvestmentTransaction`
- **Контекст:** §4.13–§4.15. Количество/цены — Decimal/TEXT произвольной точности. `decisionId?`,
  `financialCycleId?`.
- **Действия:** таблицы + репозитории + сущности; enum типов актива/сделки/причин (§2.13).
- **DoD:** CRUD актива/холдинга/сделки на in-memory БД.
- **Зависимости:** E5.T1

### E8.T2 — Движок `HoldingCostBasis` (weighted-average)
- **Контекст:** §0.1 п.2, §5.3–§5.4.
- **Действия:** пересчёт `averagePrice`/`quantity`/`investedAmount` при покупке; при продаже — уменьшение
  количества, `realizedResult = (sellPrice − averagePrice) × qtySold`, учёт комиссии; запрет продажи
  больше, чем в наличии (Failure).
- **DoD:** тесты: серия покупок по разным ценам → корректная средняя; частичная продажа → корректный
  realized; продажа сверх остатка → Failure.
- **Зависимости:** E8.T1

### E8.T3 — Экран инвестиций и карточка актива; ввод сделки
- **Контекст:** §3.11, §3.12, §2.13, §6.7.2 doc.md. Целевые/фактические доли — вывод отклонения без
  авто-рекомендаций (§2.12). Экран «Активы» в `design/mockups-overview-strip.png` — референс верстки
  (donut-диаграмма структуры, список холдингов с количеством/средней ценой/стоимостью/%).
- **Действия:** экран портфеля (стоимость/внесённый/результат/комиссии, структура по активам, целевые vs
  фактические доли), карточка актива, форма покупки/продажи. Покупка атомарно: ↓ кэш брокерского счёта,
  ↑ холдинг (через `HoldingCostBasis`), запись снапшота капитала. **Инвариант §0.1 п.5 соблюдать строго.**
- **DoD:** можно добавить брокерский счёт, купить и продать актив, увидеть P&L; капитал не задваивается
  (проверка `NetWorthCalculator` до/после покупки — сумма неизменна, меняется только структура).
- **Зависимости:** E8.T2, E5.T3

### E8.T4 — Онбординг шаг «инвестиции» (опционально)
- **Действия:** шаг с возможностью пропуска (§3.3 шаг 6).
- **DoD:** шаг пропускается или заводит брокерский счёт/актив.
- **Зависимости:** E8.T3, E2.T2

---

# E9 — Финансовые решения и маршрут денег

### E9.T1 — Таблицы `FinancialDecision`, `FinancialOperation`
- **Контекст:** §4.16, §4.17, §2.14, §2.15. Границы агрегата — §0.1 п.6 (operation = только перевод кэша).
- **Действия:** таблицы + репозиторий + сущность; state machine статусов решения (§2.14: draft→planned→
  inProgress→awaitingOperation→awaitingDocument→completed/cancelled/archived); операции с sequenceIndex,
  source/destination account, commission, курс.
- **DoD:** тесты: создание решения с цепочкой операций; допустимые переходы статуса; перевод между счетами
  не считается доходом/расходом, но комиссия уменьшает капитал (§5.2).
- **Зависимости:** E5.T1, E8.T1

### E9.T2 — Мастер создания решения + карточка решения
- **Контекст:** §3.15 (6 шагов), §3.14, §6.7.2 doc.md. Экран «Журнал операций» в
  `design/mockups-overview-strip.png` (фильтры Все/Покупки/Продажи/Переводы, лента по датам с
  иконкой/суммой/временем/счётом) — референс для детального журнала операций внутри карточки
  решения и/или ленты §2.18; отдельной вкладки под это не заводим (см. §6.7.2 doc.md).
- **Действия:** пошаговый мастер (тип → метаданные → маршрут/счета → актив → причина/документы →
  подтверждение); карточка решения с маршрутом (упрощённо — список шагов, §2.15), чек-листом, сменой
  статуса. Покупка внутри решения = `InvestmentTransaction` с `decisionId` (не дублировать как operation).
- **DoD:** user flow §3.24 (ПУМБ→Wise→IBKR→VWCE) проходится целиком: перевод(ы) + покупка, каждый шаг
  отмечается, решение завершается; счета и холдинг обновлены один раз каждый.
- **Зависимости:** E9.T1, E8.T3

---

# E10 — Документы

### E10.T1 — Пакеты и хранилище файлов
- **Действия:** добавить `file_picker` + средство просмотра PDF (согласовать пакет с пользователем перед
  установкой — §0.2); хранение файлов в app-private директории; `platform/files/`.
- **DoD:** файл выбирается и копируется в приватное хранилище; путь сохраняется.
- **Зависимости:** E0.T2

### E10.T2 — Таблицы `Document`, `DocumentLink` + integrity
- **Контекст:** §4.18, §4.19, §5.10. Полиморфная связь — целостность на уровне приложения (анализ §1.3).
- **Действия:** таблицы + репозиторий; сервис привязки документа к сущностям (решение/операция/счёт/актив/
  доход/цикл); каскад/предупреждение при удалении (§5.10); дедуп по checksum. `ImportSource='manual'` (compat №8).
- **DoD:** документ привязывается к ≥2 сущностям; удаление предупреждает о связях; дубликат по checksum ловится.
- **Зависимости:** E10.T1, E9.T1

### E10.T3 — Экран документов и просмотр
- **Контекст:** §3.16 (без авто-распознавания — §2.17 отложен). Экран «Документы» в
  `design/mockups-overview-strip.png` — прямой референс верстки (поиск, фильтр по источнику,
  группировка по месяцам, карточка документа со связанной операцией/суммой).
- **Действия:** список с фильтрами (источник/тип/период/связь), импорт, просмотр PDF/изображения, прикрепление
  к решению/операции.
- **DoD:** к решению из E9 можно прикрепить документ и открыть его позже.
- **Зависимости:** E10.T2

---

# E11 — Dashboard и аналитика

### E11.T1 — Пакет графиков (абстракция)
- **Действия:** добавить `fl_chart` за абстракцией `core/charts/` (§7.13 doc.md — заменяемость библиотеки).
- **DoD:** тестовый график рендерится через абстракцию, а не напрямую fl_chart в экране.
- **Зависимости:** E0.T2

### E11.T2 — Dashboard
- **Контекст:** §3.4, §2.22, §6.7.1 doc.md. Читать только через Application-провайдеры (compat №9).
  `design/dashboard-detail.png` — основной верстка-референс, использовать как приоритетный источник
  деталей (см. §6.7.1 doc.md по категории «Другое» в структуре капитала).
- **Действия:** карточка капитала (+ график из `net_worth_snapshots`), структура капитала, план месяца,
  ближайшие события, цели, счета, незавершённые решения, скрытие сумм (маскирование).
- **DoD:** Dashboard отражает реальное состояние из E4–E9 без ручного пересчёта; график капитала строится
  из снапшотов.
- **Зависимости:** E11.T1, E5.T3, E6.T2, E7.T2, E8.T3, E9.T2

### E11.T3 — Экран аналитики (минимальный)
- **Контекст:** §3.20 — только капитал, план/факт, структура портфеля (остальное — пост-MVP).
- **Действия:** 3 представления с выбором периода.
- **DoD:** три графика отображают корректные данные за выбранный период.
- **Зависимости:** E11.T2

---

# E12 — Планировщик, уведомления, безопасность

### E12.T1 — Таблица `Reminder` + планировщик
- **Контекст:** §4.23, §2.21, §3.18. Экран «Планировщик» в `design/mockups-overview-strip.png` —
  референс верстки (вкладки События/Напоминания, группировка по месяцам, блок «Регулярные
  напоминания» с человекочитаемым описанием повторения).
- **Действия:** таблица + репозиторий; генерация напоминаний из событий цикла/решений; экран планировщика
  (список/календарь).
- **DoD:** напоминание создаётся, отображается, ведёт в нужный экран (§2.21).
- **Зависимости:** E4.T4

### E12.T2 — Локальные уведомления
- **Действия:** согласовать и добавить `flutter_local_notifications`; уведомления: ожидание зарплаты,
  шаги решения, закрытие цикла; отмена при выполнении (§5.12); тон §6.6 и §2.24 («ничего делать не нужно»).
- **DoD:** приходит уведомление по реальному сценарию, тап ведёт в нужный экран; выполненное действие
  отменяет уведомление.
- **Зависимости:** E12.T1

### E12.T3 — Безопасность (базовый набор)
- **Контекст:** §5.13. Для MVP: БД полагается на песочницу iOS (без SQLCipher — подтверждено); документы
  в app-private; secure_storage только для флагов.
- **Действия:** согласовать и добавить `flutter_secure_storage` + `local_auth`; биометрическая блокировка,
  auto-lock, маскирование сумм.
- **DoD:** приложение блокируется биометрией; маскирование сумм работает на Dashboard.
- **Зависимости:** E11.T2

---

# E13 — Edge cases, тесты, стабилизация

### E13.T1 — Экспорт/бэкап данных пользователя
- **Контекст:** анализ §1.3 — критично, т.к. нет сервера. (Поднято раньше плана MVP_PLAN.)
- **Действия:** экспорт всей БД + документов в один файл (архив/JSON), ручной импорт с валидацией версии схемы.
- **DoD:** экспорт → удаление данных → импорт восстанавливает состояние.
- **Зависимости:** все таблицы (E1–E12)

### E13.T2 — Экран настроек
- **Контекст:** §3.21 doc.md. Ни один эпик E0–E12 не создаёт этот экран, хотя `MoreScreen` (E0.T5)
  уже ссылается на него как на пункт списка — закрываем пробел в плане. В MVP входят только разделы,
  под которые уже есть данные/функциональность: профиль, валюты, доходы, финансовые правила,
  уведомления, безопасность, данные (экспорт/импорт). Разделы «синхронизация» и «семейный доступ» не
  строим — это осознанно отложено (§8.11, §8.12 doc.md, MVP_PLAN §2 «Осознанно вне MVP»).
- **Действия:** экран-список разделов; под-экраны/диалоги: редактирование `UserProfile` (E2.T1) —
  имя, страна, часовой пояс, основная/доп. валюты, формат чисел/дат, день начала финансового месяца;
  список источников дохода (переход к E3.T1); список правил распределения (переход к E6.T1);
  настройки уведомлений по типу вкл/выкл (E12.T2); переключатели биометрии и маскирования сумм
  (E12.T3); экспорт/импорт и удаление локальных данных (E13.T1).
- **DoD:** профиль редактируется, изменения отражаются в остальном приложении (например, смена
  основной валюты); экспорт/импорт и удаление данных доступны из этого экрана.
- **Зависимости:** E2.T1, E3.T1, E6.T1, E12.T2, E12.T3, E13.T1

### E13.T3 — Приоритетные edge cases §5.14
- **Действия:** обработать: зарплата раньше/позже/частями/иная сумма; отменённый перевод; неизвестный курс
  (stale-пометки); незавершённый прошлый цикл; продажа части актива; дубликат операции.
- **DoD:** для каждого — тест или явная UI-обработка; приложение не падает и не искажает капитал.
- **Зависимости:** E13.T1

### E13.T4 — Integration-тесты основных flow
- **Контекст:** §3.22–§3.26.
- **Действия:** integration_test для: первый запуск/онбординг; получение зарплаты; инвестирование через
  несколько счетов; закрытие цикла.
- **DoD:** 4 flow зелёные в `flutter test integration_test`.
- **Зависимости:** E13.T3

### E13.T5 — Приёмка MVP по чек-листу
- **Контекст:** [MVP_PLAN.md](MVP_PLAN.md) §7 (Definition of Done MVP).
- **Действия:** пройти все 10 пунктов DoD MVP на симуляторе на реальных тестовых данных; собрать список
  расхождений.
- **DoD:** все 10 пунктов §7 MVP_PLAN выполнены; приложение работает офлайн.
- **Зависимости:** E13.T4

---

# E14 — Готовность к дистрибуции и эксплуатации

**Контекст:** doc.md §11 — процессы, дешёвые сейчас и дорогие при ретрофите позже. Это не часть
MVP-функциональности, но нарушение обычного порядка эпиков здесь осознанное: несколько задач
(E14.T1, E14.T3, E14.T6) стоит делать рано, не дожидаясь E2–E13 — цена задержки растёт с каждым
днём, а не остаётся постоянной, в отличие от большинства других задач плана.

### E14.T1 — Crash reporting
- **Контекст:** doc.md §7.19, §11.1. Чем раньше подключено, тем больше истории накоплено к
  моменту, когда она понадобится — не откладывать до диапазона E10–E12.
- **Действия:** согласовать с пользователем пакет (Sentry или Firebase Crashlytics — новая
  сторонняя зависимость); инициализация в `main.dart`; перехват необработанных ошибок
  (`FlutterError.onError` + `PlatformDispatcher.instance.onError`); явное правило в коде и в
  BUILD_PLAN §0.4 — никаких сумм/документов/токенов/личных данных в отправляемых событиях
  (совпадает с §5.13 doc.md).
- **DoD:** тестовый краш долетает до дашборда сервиса; правило о запрете чувствительных данных
  зафиксировано рядом с местом инициализации.
- **Зависимости:** E0.T2

### E14.T2 — Канал обратной связи
- **Контекст:** doc.md §11.2. Не зависит от полного экрана «Настройки» (E13.T2, который придёт
  поздно) — достаточно одного пункта в уже существующем `MoreScreen` (E0.T5).
- **Действия:** согласовать пакет для открытия почтового клиента (например `url_launcher` —
  новая сторонняя зависимость); пункт «Обратная связь» в `MoreScreen`, открывающий `mailto:` с
  преднастроенной темой (например, версия приложения в теле письма для контекста).
- **DoD:** тап по пункту открывает почтовый клиент с корректным адресатом и темой.
- **Зависимости:** E0.T5

### E14.T3 — Privacy Policy (заглушка)
- **Контекст:** doc.md §11.3, §8.14. Блокер, а не открытый вопрос без срочности: Apple не
  позволяет раздать даже TestFlight-сборку без действующей ссылки на политику конфиденциальности
  в App Store Connect.
- **Действия:** написать минимальный, честный текст (данные хранятся локально на устройстве, не
  передаются на сервер за исключением [что реально верно на момент публикации], как удалить
  данные, контакт для вопросов); разместить как статическую страницу (например, GitHub Pages
  из этого репозитория — не требует отдельного хостинга).
- **DoD:** публичный URL с политикой существует, готов к вставке в App Store Connect.
- **Зависимости:** нет — можно делать в любой момент, полностью самостоятельная задача.

### E14.T4 — Политика телеметрии (решение)
- **Контекст:** doc.md §11.4. Решение, не реализация — сама аналитика (SDK) в MVP не ставится.
- **Действия:** зафиксировать в doc.md §11.4 конкретный список разрешённых событий (например,
  переходы между вкладками, обезличенные) и явный список запрещённых данных (совпадает с §5.13:
  суммы, документы, личные комментарии).
- **DoD:** список зафиксирован текстом; ничего не реализуется.
- **Зависимости:** нет.

### E14.T5 — Пайплайн релиза (Fastlane + TestFlight)
- **Контекст:** doc.md §7.18, §11.5. Не срочно при тестировании только на симуляторе самим
  разработчиком; срочно в момент, когда появляется первый внешний тестировщик.
- **Действия:** Fastlane lane для сборки и загрузки в TestFlight; запись приложения в App Store
  Connect (требует активного Apple Developer Program — решение и оплата на стороне пользователя,
  не техническая задача); credentials/API-ключи Fastlane — в `.gitignore` сразу, по прецеденту
  §0.4 (Team ID).
- **DoD:** `fastlane beta` (или аналог) успешно заливает сборку в TestFlight.
- **Зависимости:** E14.T1, E14.T3

### E14.T6 — Стратегия секретов под будущий бэкенд
- **Контекст:** doc.md §11.6. Прецедент — реальный Apple Developer Team ID утёк в первый коммит
  через `project.pbxproj` (исправлено, см. §0.4). Решение, не полная реализация — бэкенда
  (Supabase, §7.10 doc.md) пока нет.
- **Действия:** зафиксировать правило (секреты только через `--dart-define` или локальный
  gitignored `.env`, никогда не в репозитории) рядом с существующим правилом §0.4 о чувствительных
  данных; превентивно добавить `*.env`/`*.env.local` в `.gitignore`, даже без реального `.env`
  сейчас.
- **DoD:** правило зафиксировано в §0.4; `.gitignore` обновлён превентивно.
- **Зависимости:** нет.

---

# Приложение A. Порядок для агента-исполнителя

1. Всегда начинай с чтения раздела 0 этого файла и CLAUDE.md.
2. Бери следующую невыполненную задачу по номеру; проверь её зависимости выполнены.
3. Перед изменениями — прочитай затрагиваемый код (правило CLAUDE.md «inspect before proposing»).
4. Для нового пакета вне §0.2 — спроси пользователя.
5. По завершении — прогони общий DoD (§0.5) и частный DoD задачи.
6. Не коммить/не пуши без явной просьбы.
7. Спорную семантику расчётов (проценты в allocation, привязка дохода) уточняй у пользователя, а не
   угадывай — это финансовая корректность.
8. Исключение из «эпики по порядку»: E14 (готовность к дистрибуции) можно и стоит начинать раньше
   своего номера — E14.T3/T4/T6 не имеют зависимостей вовсе, E14.T1/T2 зависят только от E0.
   Это осознанное исключение, зафиксированное в контексте самого эпика, не повод игнорировать
   правило для остальных эпиков.
